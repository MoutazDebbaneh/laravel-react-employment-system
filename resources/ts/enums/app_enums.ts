export enum Locale {
    English = 'en',
    Arabic = 'ar',
};

export enum Role {
    RootAdmin = 1,
    Admin = 2,
    User = 3,
    Company = 4,
};

export enum SourceType {
    Internal = 1,
    Scrape = 2,
    API = 3,
};